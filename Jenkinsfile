pipeline {
    agents any {}

    stages {
        stage('Build') {
            // Build the container
        }
        stage('Test') {
            // Placeholder for testing code
        }
        stage('Push to registry') {
            // Push to registry
        }
        stage('Deplay to environment') {
            // Deploy build to env
        }
    }
}