pipeline {
    agent any {}

    stages {
        stage('Build') {
            steps {
                // Build the container
            }
        }
        stage('Test') {
            steps {
                // Placeholder for testing code
            }
        }
        stage('Push to registry') {
            steps {
                // Push to registry
            }
        }
        stage('Deplay to environment') {
            steps {
                // Deploy to env
            }
        }
    }
}